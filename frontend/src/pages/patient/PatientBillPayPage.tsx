export function PatientBillPayPage() {
  return (
    <section>
      <h2>Bill Pay (Simulation)</h2>
      <p>Payment simulation is handled in frontend only (no backend changes).</p>
    </section>
  );
}
